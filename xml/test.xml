<?xml version="1.0" encoding="utf-8"?>
<Shader title="Shader/Default">
  <Type name="Geometry">

  </Type>
  <Type name="Vertex">#version 330 core
    layout (location = 0) in vec3 pos;
    layout (location = 1) out vec3 color;
    layout (location = 2) out vec2 texCoord0;

    void main()
    {
      //gl_Position = vec4(pos, 1.0);
    }
  </Type>
  <Type name="Fragment">#version 330 core
    out vec4 FragColor;
    in vec3 pos;
    in vec3 color;
    in vec2 texCoord0;

    int main()
    {
      FragColor = vec4(color, 1.0);
    }
  </Type>

</Shader>
